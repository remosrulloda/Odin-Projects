<!-- index.ejs -->
<html>

<head>
    <title>Homepage</title>
</head>

<body>
    <%- include('navbar', {links: links}) %>
        <!-- index.ejs -->
        <ul>
            <% users.forEach((user)=> { %>
                <%- include('user', {user: user}); %>
                    <% }); %>
        </ul>

</body>

</html>